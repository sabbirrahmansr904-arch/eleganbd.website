rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ===============================================================
    // Helper Functions
    // ===============================================================
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isAuthenticated() && (
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin') ||
        (request.auth.token.email == "sabbirrahmansr904@gmail.com")
      );
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // ===============================================================
    // Rules
    // ===============================================================
    
    // Products: Anyone can read, allow products management
    match /products/{productId} {
      allow read, write: if true;
    }

    // Banners: Anyone can read, allow admin or authenticated app to manage banners
    match /banners/{bannerId} {
      allow read, write: if true;
    }

    // Settings: Anyone can read, allow admin or authenticated app to update settings
    match /settings/{settingId} {
      allow read, write: if true;
    }

    // Coupons: Anyone can read, only admin can write
    match /coupons/{couponId} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Reviews: Anyone can read and create, only admin can delete/update
    match /reviews/{reviewId} {
      allow read: if true;
      allow create: if true; // Allow anonymous reviews
      allow update, delete: if isAdmin();
    }

    // Orders: Allow read and write so checkout, order tracking, and admin order management work seamlessly
    match /orders/{orderId} {
      allow read, write: if true;
    }

    // Customers: Anyone can create customer profile, admin/app can read and manage
    match /customers/{customerId} {
      allow read, write: if true;
    }

    // Users (Roles): Users can read and update their own profile, admin can read/write all
    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create, update: if isOwner(userId) || isAdmin();
      allow delete: if isAdmin();
    }
  }
}
